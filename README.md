# firmware-config-skill

`firmware-config` standardizes embedded firmware configuration across board-targeted, hardware-backed, and provisioning-oriented repos.

It separates project constants, hardware selectors, build versions, runtime provisioning, and local-only values so firmware projects stay buildable, flashable, and understandable across boards.
